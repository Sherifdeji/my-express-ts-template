# Environment Configuration
NODE_ENV=       # Options: 'development', 'production'
PORT=           # The port your server will listen on
HOST=           # Hostname for the server

# Database Configuration
DATABASE_URL=   # The connection string for your database

# JWT Configuration
JWT_SECRET=     # The secret used to sign JWT tokens
JWT_EXPIRATION= # The expiration time for JWT tokens (e.g., '1h', '2d')